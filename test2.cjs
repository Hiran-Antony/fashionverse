const fetch = require('cross-fetch'); const key = require('dotenv').config().parsed.VITE_GEMINI_API_KEY; async function test(model) { const url = 'https://generativelanguage.googleapis.com/v1beta/models/' + model + ':generateContent?key=' + key; const res = await fetch(url, { method: 'POST', headers: {'Content-Type': 'application/json'}, body: JSON.stringify({contents:[{parts:[{text:'hi'}]}]})}); const data = await res.json(); console.log('[' + model + '] Error:', JSON.stringify(data.error)); } async function run() { await test('gemini-2.5-flash'); await test('gemini-1.5-flash'); await test('gemini-2.0-flash-exp'); await test('gemini-1.5-pro'); } run();
